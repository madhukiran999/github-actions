name: 05-01-filter-activity-types

on: 
  pull_request:
    types:
      - opened
      - synchronize
    branches:
    - main

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - run: echo "run only on main, when PR is opened or synchronized"

